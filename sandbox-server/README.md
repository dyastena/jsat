# Sandbox Server (removed)

The sandbox server has been removed from this repository to keep the project focused on frontend work.

If you need to run a code execution service later, consider using a dedicated repository or a hosted judge (e.g., Judge0). To restore the original sandbox, retrieve the files from repository history.
